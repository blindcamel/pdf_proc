Return only a single dictionary of key:value pairs. No other text.
Extracted data should be returned as key-value pairs contained within a properly formatted Python dictionary with unique keys.
Each key is a tuple ((document_id, page_number)).
document_id starts at 1 and increments with each new document detected in the input text.
page_number corresponds to the page number of the specific document.

shortnames.py is available to you. 

Steps of Operation:
1. Identify 'Company Name'.
2. !!!IMPORTANT!!! Iterate over the keys in shortnames.py and check for a partial match to 'Company Name'.   If a partial or full match is found, replace 'Company Name' with its corresponding value in shortnames.py.  If no match is found, 'Company Name' remains unchanged.
3. Identify Purchase_Order#' for each document in the pdf.  six digits immediately followed by 2 or more letters with no spaces in between. example: '123456ab'
4. Verify 'Purchase_Order#' accuracy via criteria below.
5. Identify 'Invoice_Number'for each document in the pdf.
6. Verify the number of unique invoice numbers matches the number of documents
5. Ensure proper extraction and format of each section, using the syntax suitable for evaluation functions when required.  Confirm each key and value follows the stipulated guidelines.
6. Return data as instructed above.

Output Format
[
    {
        (1, 1): ["CompanyA", "Purchase_Order#", "Invoice_Number"],
        (1, 2): ["CompanyA", "Purchase_Order#", "Invoice_Number"],
        (2, 1): ["CompanyA", "Purchase_Order#", "Invoice_Number"]
    }
]
The dictionary should follow these guidelines:

Keys: Tuples (document_id, page_number)

document_id: Each unique Invoice_Number represents a new document and should have a unique document_id. If a subsequent page has a different Invoice_Number, then that is the start of a new document and thus should have the next document_id.
page_number: Page sequence within the pages that constitute that specific document, where each page has the same document_id.
Values: Lists of extracted strings in this order:

Company Name
Purchase Order Number
Invoice Number
If extraction is uncertain, the value should be replaced with "error". Ensure syntax is valid for ast.literal_eval().

Company Name
Should be extracted from the header or footer, where the actual company issuing the invoice is mentioned.
Use keywords like "REMIT TO" or "MAIL REMITTANCE TO" to locate the company name, as these sections typically contain the issuer's details.
Capitalization is retained.
Spaces are removed: **"The Parts Works → "ThePartsWorks".
Only letters; no special characters.

!!!IMPORTANT!!! Iterate over the keys in shortnames.py and check for a partial match to 'Company Name'.   If a partial or full match is found, replace 'Company Name' with its corresponding value in shortnames.py.  If no match is found, 'Company Name' remains unchanged.

Purchase Order Number
Always 6 digits followed by 2+ letters. null.
Convert letters to lowercase.

Invoice Number
Extracted as the text string closest to the phrase "Invoice Number".
If extraction is uncertain, "error" is returned.

Feedback Mechanism: Log any discrepancies or errors for analysis and improvement of the extraction process.